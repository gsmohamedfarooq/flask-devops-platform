name: Docker Test

on:
  workflow_dispatch:

jobs:
  docker-test:
    runs-on: arc-runner-set

    steps:
      - name: Check Docker
        run: |
          docker version
          docker info

      - name: Run Docker test
        run: |
          docker run --rm hello-world
